# from joblib import Parallel, delayed
# from scipy.optimize import least_squares
#
#
# def optimize_single_case(args):
#     return least_squares(your_func, your_x0, args=args)
#
#
# results = Parallel(n_jobs=4)(delayed(optimize_single_case)(a) for a in all_args)
#
